const mainPhotoContainer = document.querySelector(".photos--view");

/*export const renderPhotos = function () {
  const fileArr = addHandlerChooseImages();
  console.log(fileArr);
  fileArr.forEach(function (file) {
    console.log(file);
    if (file.type === "image/jpeg" || file.type === "image/png") {
      console.log(file.webkitRelativePath);
    }
  });
};*/
